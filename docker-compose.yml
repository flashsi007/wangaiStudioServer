version: "3.9"

services:
  admin-wangai-studio:
    build: .
    container_name: admin-wangai-studio
    image: admin-wangai-studio:latest
    restart: "no"               # 构建完即退出
    volumes:
      # 把容器里的 /dist 挂到宿主机 Nginx 的站点目录
      - /var/www/vue-app:/dist:ro